source "https://rubygems.org"

# GitHub Pages compatible build (pins Jekyll + whitelisted plugins,
# including jekyll-remote-theme used by Minimal Mistakes).
gem "github-pages", group: :jekyll_plugins
gem "jekyll-include-cache", group: :jekyll_plugins

# Needed on Ruby 3.x for `jekyll serve` locally.
gem "webrick"
